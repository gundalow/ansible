---
- hosts: veos01
  gather_facts: no
  connection: local
  tasks:
    - name: enable cli on remote device
      eos_eapi:
        enable_http: yes
        enable_https: yes
        enable_local_http: yes
        enable_socket: yes
        provider: "{{ cli }}"
